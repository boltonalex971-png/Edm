# Host Console

Operate the platform itself: live status of running tasks and drivers, log streams from the host process, plugin health, and a reference for the platform's installed components.

Where an admin or operator goes to know what the host is doing right now and intervene when it isn't doing what it should.

The Console is the inside view of the shell — the rest of the modules are the work; this one is the machinery that runs the work.
